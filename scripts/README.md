# Code

Project 8 should use runnable evidence before final findings are written.

Recommended local fallback if native ROS 2 evidence is unavailable:

```text
- occupancy-grid scenario generator
- LiDAR-like obstacle observation model
- A* or DWA-style path planner
- controlled noise/obstacle-density/occlusion conditions
- deterministic CSV metrics and figures
```

If actual ROS 2/Gazebo/Nav2 logs are provided later, place them under `data/raw` and convert analysis outputs to `data/processed`.
